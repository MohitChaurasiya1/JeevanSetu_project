# JeevanSetu Backend API

Django REST Framework backend providing RESTful endpoints, JWT authentication, PostgreSQL integration, and machine learning inference.

## Quickstart
```bash
python -m venv venv
# Activate virtual environment
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver
```
